import { vars } from '~/theme'

export const styles = {
  'issue-page__title': { mb: 2, color: vars.text },
  'issue-page__tabs': { mb: 3, borderBottom: `1px solid ${vars.border}` },
  'issue-page__no-attachment': { color: vars.text2 },
}
