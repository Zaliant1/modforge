import { vars } from '~/theme'

export const styles = {
  'root': {
    minHeight: '100vh',
    bgcolor: vars.bg,
    color: vars.text,
    fontFamily: vars.ui,
    fontSize: 14,
    WebkitFontSmoothing: 'antialiased',
  },
}
